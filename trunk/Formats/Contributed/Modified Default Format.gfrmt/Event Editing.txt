!SJGR changed from default to move place here from child window

load TagBlock

if #msg="Media"
  Gosub TagBlock,"OBJE","Media"
  hideexcess
  
else
newline

! header on first event
if #first="yes"
  cell static,local("EVEN")
  set tabwidth 1 alignment center border square
  cellBackground "Table Cell Background"

  cell static,"?"
  set tabwidth 1 alignment center border square
  cellBackground "Table Cell Background"

  cell static,local("DATE")
  set tabwidth 1 alignment center border square
  cellBackground "Table Cell Background"
  
  hskip -24
  cell LinkButton
  menulink "Sort Events"
  set tabwidth 1 border none image "go-down" imageHeight 12
  help local("Sort the events")
  
  ! Next block added to display place detail also needs tabs settings changed in INDI_Events, INDI_All, FAM_Events & FAM_All templates  SJGR
 
  cell static,local("PLAC")
  set tabwidth 1 alignment center border square
  cellBackground "Table Cell Background"


  newline
  #first="no"
endif
  
! event name, check box, and date
if @tag="EVEN"
  #eName=@TYPE
  if #eName=""
	#eName=local(@tag)
	cell static,#eName
  else
	cell expression,@TYPE
    localize true
  endif
else
  #eName=local(@tag)
  cell static,#eName
endif
set tabwidth 1 alignment center border square
cellBackground "Table Cell Background"

cell CheckBoxEdit
set tabwidth 1
if local("<LANG>")="English"
  help "Check if "&local(@tag)&" occurred but nothing else known"
else
  help local("Check if event occurred but nothing else known")
endif

show DATE Event
show PLAC Event

! rest in a child window
cell StructuresLink,EventDetails,@tag
set border no offset 3 width 22
if more
  set image "details_here"
else
  set image "details"
endif
LinkAll all

#tagKey=@tag&" details"
help local("Click to edit")&": "&local(#tagKey)
endif